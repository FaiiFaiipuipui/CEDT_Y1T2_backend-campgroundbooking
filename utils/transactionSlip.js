const convertData = (bufferData) => {

    const base64Data = Buffer.from(bufferData).toString('base64'); //Convert User inputdata to base64

    return base64Data
};

const compressImage = () => {
    
}
